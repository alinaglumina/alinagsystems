import { ContactView } from './contact-view';

export const metadata = {
  title: 'Contact',
  description: "Tell us about your project. We'll come back with a clear, honest plan — usually within one business day.",
};

export default function ContactPage() {
  return <ContactView />;
}
